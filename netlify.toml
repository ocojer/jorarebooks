[build]
  command = "node build.js"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

# index.html is generated by build.js on every deploy (pulls the latest
# Rare Book Chat episode from the RSS feed), so it isn't committed to git.
